//foreach a function is apply on every element in array 
const colors = ['red', 'green', 'blue'];

colors.forEach(function(currentValue,index, array){
    console.log(index, currentValue); 
});

/* 
0 red
1 green
2 blue
*/ 
